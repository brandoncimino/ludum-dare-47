using UnityEngine;

public class GithubTestScript : MonoBehaviour {
    // Start is called before the first frame update
    // test if I can push to the repository
    void Start() { }

    // Update is called once per frame
    void Update() { }
}